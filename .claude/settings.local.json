{
  "permissions": {
    "allow": [
      "Bash(python manage.py shell -c \"from django.contrib import admin; from chat.models import Room; list\\(Room.objects.all\\(\\)\\)\")",
      "Bash(python manage.py shell -c \"from chat.models import Room; from accounts.models import User; print\\([\\(r.name, r.members.count\\(\\)\\) for r in Room.objects.all\\(\\)[:10]]\\)\")",
      "Bash(python manage.py shell -c \"from chat.models import Room; from django.contrib.admin import site; from chat.admin import RoomAdmin; ra = RoomAdmin\\(Room, site\\); list\\(ra.get_queryset\\(None\\)\\)\")",
      "Bash(python manage.py shell -c \"from chat.models import Room; from accounts.models import User; users = set\\(User.objects.values_list\\('id', flat=True\\)\\); bad = []; [bad.append\\(\\(r.name, [m.id for m in r.members.all\\(\\) if m.id not in users]\\)\\) for r in Room.objects.all\\(\\) if any\\(m.id not in users for m in r.members.all\\(\\)\\)]; print\\(bad\\)\")",
      "Bash(python manage.py shell -c \"from chat.models import Room; rooms = Room.objects.filter\\(admin__isnull=False\\).select_related\\('admin'\\); [print\\(f'{r.name}: admin_id={r.admin_id}, admin={r.admin}'\\) for r in rooms[:5]]\")",
      "Bash(python manage.py shell -c \"from django.db import connection; cursor = connection.cursor\\(\\); cursor.execute\\('PRAGMA foreign_keys = OFF'\\); cursor.execute\\('DELETE FROM chat_room WHERE id = 6'\\); print\\('Deleted!'\\); cursor.execute\\('PRAGMA foreign_keys = ON'\\)\")"
    ]
  }
}
